name: ecommerce_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_native_splash: ^2.4.7


  firebase_core:
  firebase_auth:
  cloud_firestore:
  provider:
  # ----------------------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # This path looks correct based on your file
  assets:
    - assets/images/appstore.png

# --- I've fixed the indentation in this block ---
flutter_native_splash:
  color: "#FFFFFF" 
  image: assets/images/appstore.png 
  android_12:
    color: "#FFFFFF"
    image: assets/images/appstore.png
  
  # This 'web: true' was mis-indented; I have fixed it.
  web: true